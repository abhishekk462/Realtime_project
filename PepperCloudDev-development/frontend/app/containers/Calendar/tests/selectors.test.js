// import { fromJS } from 'immutable';
// import { makeSelectCalendarDomain } from '../selectors';

// const selector = makeSelectCalendarDomain();

describe('makeSelectCalendarDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
