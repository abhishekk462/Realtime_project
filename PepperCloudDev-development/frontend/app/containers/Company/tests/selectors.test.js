// import { fromJS } from 'immutable';
// import { makeSelectCompanyDomain } from '../selectors';

// const selector = makeSelectCompanyDomain();

describe('makeSelectCompanyDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
