// import { fromJS } from 'immutable';
// import { makeSelectAppGeneratorDomain } from '../selectors';

// const selector = makeSelectAppGeneratorDomain();

describe('makeSelectLeadDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
