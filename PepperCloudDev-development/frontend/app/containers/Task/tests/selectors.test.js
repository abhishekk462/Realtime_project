// import { fromJS } from 'immutable';
// import { makeSelectTaskDomain } from '../selectors';

// const selector = makeSelectTaskDomain();

describe('makeSelectTaskDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
